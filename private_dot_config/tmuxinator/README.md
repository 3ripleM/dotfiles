Tmuxinator project files for this dotfiles repo.

Place tmuxinator YAML project files here. On macOS the Fish config will symlink this directory to ~/.tmuxinator.

Example: `example.yml` is included as a starter project.
